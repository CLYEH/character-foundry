# ========================
# Python
# ========================
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
venv/
env/
.venv/
*.egg-info/
dist/
build/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ========================
# React / Node.js
# ========================
node_modules/
.pnpm-store/
dist/
build/
.next/
.nuxt/
.cache/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.pnp
.pnp.js
*.tsbuildinfo

# ========================
# Environment variables
# ========================
.env
.env.local
.env.*.local

# ========================
# OS generated files
# ========================
.DS_Store
Thumbs.db

# ========================
# Editor
# ========================
.idea/
.vscode/
*.suo
*.sw?

# ========================
# Playwright (T-012)
# ========================
web/playwright-report/
web/test-results/
web/playwright/.cache/

# ========================
# Local tooling / MemPalace
# ========================
chroma.sqlite3
entities.json
mempalace.yaml
.mempalace/

# ========================
# Mutation testing (T-060)
# ========================
# `mutmut run` writes a sandbox copy of the codebase + per-mutant metadata
# into `api/mutants/`. The sandbox is fully reproducible from
# `[tool.mutmut]` in `api/pyproject.toml`; checking it in just bloats
# the repo. The committed `.harness/mutation-baseline.json` is the only
# artifact the workflow needs to keep across runs.
api/mutants/
api/drift-report.txt
.coverage
api/.coverage
api/coverage.xml

# .claude/: ignore everything by default, but track the project-shared
# settings, agents, and hooks (per-user state like settings.local.json
# and scheduled_tasks.lock stay out).
.claude/*
!.claude/settings.json
!.claude/agents
!.claude/agents/**
!.claude/hooks
!.claude/hooks/**
